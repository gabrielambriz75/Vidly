@model Vidly.ViewModels.MovieFormViewModel
@{
    ViewBag.Title = @Model;
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Edit Movie</h2>


@using (Html.BeginForm("Save", "ListaPeliculas"))
{
    <div class="form-group">
        @Html.LabelFor(m =>m.Name)
        @Html.TextBoxFor(m => m.Name, new { @class = "form-control" })
    </div>
    <div class="form-group">
        @Html.LabelFor(m => m.ReleaseDate)
        @Html.TextBoxFor(m => m.ReleaseDate,"{0:yyyy-MM-dd}" , new { @class = "form-control" })
    </div>
    <div class="form-group">
        @Html.LabelFor(m => m.GenreTypeId)
        @Html.DropDownListFor(m => m.GenreTypeId, new SelectList(Model.GenreTypes, "Id", "Name"), "Select Genre Type", new { @class = "form-control" })
    </div>

    <div class="form-group">
        @Html.LabelFor(m => m.NumberStock)
        @Html.TextBoxFor(m => m.NumberStock, new { @class = "form-control" })
    </div>

    @Html.HiddenFor(m =>m.Id);
    @Html.AntiForgeryToken();
    <button type="submit" class="btn btn-primary">Save</button>
}